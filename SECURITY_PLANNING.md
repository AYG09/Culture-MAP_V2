# 🔐 보안 및 접근 제어 계획

## 현재 상태
- ✅ Firebase 기반 멀티유저 시스템 구축 완료
- ✅ Vercel 배포 성공
- ⚠️ **문제점**: 현재 아무나 접속 가능한 상태

## 🚨 접근 제어 필요성
현재 웹사이트 URL을 아는 모든 사람이 조직문화 분석 도구에 접근할 수 있음
→ **허가된 사용자만 접근할 수 있도록 인증 시스템 구현 필요**

## 🎯 구현 방안 (3가지 옵션)

### Option 1: 간단한 패스워드 시스템 (추천 - 빠른 구현)
**구현 방식:**
- 앱 시작 시 마스터 패스워드 입력 모달 표시
- 올바른 패스워드 입력 시에만 세션 관리 화면 접근 허용
- 로컬스토리지에 인증 상태 저장 (세션 유지)

**장점:**
- 구현이 간단하고 빠름
- 추가 서비스 필요 없음
- 즉시 적용 가능

**단점:**
- 패스워드가 노출되면 보안 취약
- 사용자별 개별 관리 어려움

**구현 파일:**
- `src/components/AuthModal.tsx` (새로 생성)
- `src/services/AuthService.ts` (새로 생성)
- `src/components/MultiUserApp.tsx` (수정)

### Option 2: Firebase Authentication (중장기 - 더 안전)
**구현 방식:**
- Firebase Auth 서비스 활용
- 구글 로그인, 이메일/패스워드 로그인 지원
- 허가된 이메일 주소 화이트리스트 관리
- Firebase Rules로 데이터베이스 접근 제어

**장점:**
- 매우 안전한 인증 시스템
- 사용자별 개별 관리 가능
- 구글, 페이스북 등 소셜 로그인 지원
- Firebase와 완벽 연동

**단점:**
- 구현이 다소 복잡
- Firebase Auth 서비스 설정 필요
- 사용자 관리 인터페이스 필요

**구현 파일:**
- `src/lib/firebase.ts` (Auth 설정 추가)
- `src/services/AuthService.ts` (Firebase Auth 연동)
- `src/components/LoginModal.tsx` (새로 생성)
- Firebase Rules 설정

### Option 3: Vercel 접근 제한 (네트워크 레벨)
**구현 방식:**
- Vercel Pro 계정으로 업그레이드
- IP 화이트리스트 또는 패스워드 보호 설정
- 특정 도메인에서만 접근 허용

**장점:**
- 코드 수정 없이 구현 가능
- 네트워크 레벨에서 차단
- 매우 강력한 보안

**단점:**
- Vercel Pro 계정 필요 (유료)
- 유연성 부족
- IP가 변경되면 재설정 필요

## 📱 모바일 접속 제한 문제

### 현상
모바일에서 접속 시 "구글 정책상 접속이 제한된다"는 메시지 발생

### 실제 원인 (확인됨)
**구글 보안 정책**: 모바일 앱 내 브라우저(카카오톡, 인스타그램, 페이스북 등의 WebView)에서 Firebase 접속 시 보안상 차단

**오류 메시지**: 
```
액세스 차단됨: google 정책에 의해 이 요청이 차단되었습니다. 
보안 브라우저 사용 정책을 준수하지 않았습니다.
```

### 해결 방안

#### 즉시 해결 (사용자 측)
1. **"웹브라우저에서 열기"** 버튼 클릭
2. URL 복사해서 **Chrome/Safari**에서 직접 접속  
3. **카카오톡**: 설정 → 일반 → 다른 앱에서 링크 열기 활성화

#### 개발자 측 해결 (코드 수정)
1. **Firebase 설정 개선**
   - Auth 설정에 `persistence` 옵션 추가
   - `signInWithRedirect` 대신 `signInWithPopup` 사용

2. **사용자 안내 추가**
   - 모바일 앱 브라우저 감지
   - "일반 브라우저에서 열기" 안내 메시지 표시

3. **대체 인증 방식**
   - Firebase Auth 대신 간단한 패스워드 시스템 우선 구현
   - 모바일에서도 정상 작동하는 방식 채택

## 🗓️ 구현 일정 (주말 작업)

### 1단계: 긴급 보안 (토요일)
- [ ] 간단한 패스워드 시스템 구현 (Option 1)
- [ ] 모바일 접속 제한 문제 해결

### 2단계: 모바일 최적화 (일요일)
- [ ] Firebase 도메인 설정 완료
- [ ] 모바일 브라우저 테스트
- [ ] 반응형 UI 점검

### 3단계: 향후 개선사항
- [ ] Firebase Authentication 구현 검토 (Option 2)
- [ ] 사용자 관리 시스템 설계
- [ ] 접근 로그 및 모니터링 시스템

## 💡 즉시 임시 조치

현재 당장 할 수 있는 임시 보안 조치:
1. **URL 숨기기**: 직접 링크 공유하지 않기
2. **Firebase Rules 설정**: 읽기/쓰기 권한 제한
3. **세션 코드 복잡도 증가**: 추측하기 어려운 코드 생성

## 📝 참고 링크
- [Firebase Auth 문서](https://firebase.google.com/docs/auth)
- [Vercel 보안 설정](https://vercel.com/docs/security)
- [Firebase Security Rules](https://firebase.google.com/docs/rules)

---
📅 **작성일**: 2025-08-27  
🔄 **최종 수정**: 주말 작업 후 업데이트 예정